const switchColors = document.querySelectorAll('switch')
const colorBox = document.querySelector('.background-box')

const colors = ['red','green', 'blue', 'yellow', 'white', 'cyan', 'darkGray', 'lightGray', 'lightGreen', 'lightYellow', 'lightGreen', 'voilet']

let randomColor = Math.floor(Math.random() * colors.length)



    switchColors.forEach()